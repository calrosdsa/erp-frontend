import { useLoaderData } from "@remix-run/react"
import { loader } from "../../route"


export default function ItemDashboardTab(){
    // const {stockLevel} = useLoaderData<typeof loader>()
    return (
        <div>
            {/* {JSON.stringify(stockLevel)} */}
        </div>
    )
}
